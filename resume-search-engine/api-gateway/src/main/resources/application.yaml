server:
  port: 8080 # Cổng chạy duy nhất của API Gateway tiếp nhận request từ Frontend

spring:
  application:
    name: api-gateway

  # Cấu hình cụm Redis dùng để lưu trữ bộ đếm số lượt gọi API phục vụ cho Rate Limiting
#  data:
#    redis:
#      host: localhost
#      port: 6379
#      timeout: 2000ms

# Chuỗi mật mã Secret Key toàn cục dùng để kiểm tra tính toàn vẹn của Token (Chuỗi phải dài trên 256-bit)
jwt:
  secret: "LuonTD_Super_Secret_Key_2026_For_CV_Architecture_System_Safety_Token_Validation_Key"

# Cấu hình bật Log ở chế độ hiển thị chi tiết để bạn dễ debug luồng đi nước bước của API
logging:
  level:
    org.springframework.cloud.gateway: INFO
    org.springframework.http.server.reactive: INFO